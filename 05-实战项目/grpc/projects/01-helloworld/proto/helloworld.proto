syntax = "proto3";

option go_package = "./proto;helloworld";

package helloworld;

// 定义问候服务
service Greeter {
  // 普通 RPC 方法
  rpc SayHello (HelloRequest) returns (HelloReply);
}

// 请求消息
message HelloRequest {
  string name = 1;
}

// 响应消息
message HelloReply {
  string message = 1;
}