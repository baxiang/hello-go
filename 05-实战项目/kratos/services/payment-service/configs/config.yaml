server:
  grpc:
    network: tcp
    addr: 0.0.0.0:50054
    timeout: 60

database:
  driver: mysql
  source: root:root123@tcp(localhost:3306)/ecommerce?charset=utf8mb4&parseTime=True&loc=Local
  max_open: 25
  max_idle: 5
  max_life: 300

services:
  order:
    addr: localhost:50053

logger:
  level: info
  format: json
